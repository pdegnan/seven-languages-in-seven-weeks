// Outputs 2
(1 + 1) println

// Throws an exception, "+" operator expects operands to be
// Number types, not a Sequence type like the string "one".
// Therefore, Io is a strongly typed language.
(1 + "one") println